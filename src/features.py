# features.py
import numpy as np

def extract_features(car, track):
    """
    Extract features from the car and track for the reinforcement learning model.
    Args:
        car (Car): The car object containing its state.
        track (Track): The track object containing the track data.
    Returns:
        np.ndarray: Feature vector.
    """
    # TODO: Implement feature extraction logic
    feature_vector = np.zeros(6)
    return feature_vector
    
